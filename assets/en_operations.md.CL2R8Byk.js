import{_ as i,o as a,c as e,a0 as t}from"./chunks/framework.DPl9WUxR.js";const c=JSON.parse('{"title":"Persistence diagnostics, admission limits and streaming","description":"","frontmatter":{},"headers":[],"relativePath":"en/operations.md","filePath":"en/operations.md"}'),n={name:"en/operations.md"};function o(h,s,l,d,r,p){return a(),e("div",null,[...s[0]||(s[0]=[t(`<h1 id="persistence-diagnostics-admission-limits-and-streaming" tabindex="-1">Persistence diagnostics, admission limits and streaming <a class="header-anchor" href="#persistence-diagnostics-admission-limits-and-streaming" aria-label="Permalink to &quot;Persistence diagnostics, admission limits and streaming&quot;">​</a></h1><p>These APIs are available starting in v1.6.1. Existing settings remain the default.</p><h2 id="persistence-diagnostics" tabindex="-1">Persistence diagnostics <a class="header-anchor" href="#persistence-diagnostics" aria-label="Permalink to &quot;Persistence diagnostics&quot;">​</a></h2><p>Use <code>db.get_status()</code> / <code>await db.aget_status()</code> without enabling metrics. Table children report the shared engine&#39;s state. Immediate mode has zero queue counts and <code>None</code> timestamps.</p><p>The result includes <code>mode</code>, <code>pending_kvs_count</code> (distinct staged keys), <code>flushing_kvs_count</code> (the in-flight batch), <code>pending_sql_count</code> (queued SQL, excluding running work), <code>oldest_pending_age_seconds</code> (queued work only, reset on retry), and <code>flush_active</code>. It also includes current <code>failed_count</code>, cumulative <code>failure_count</code>, cumulative <code>dropped_failure_count</code>, <code>last_failure_time</code>, and <code>last_successful_flush_time</code>. Timestamps are UNIX seconds. Empty queues have no age; an empty flush does not advance the last successful flush timestamp.</p><p>This is a best-effort diagnostic snapshot sampled under separate locks, not a durability barrier. An in-flight KVS batch can include already committed rows. Queue age collection traverses queued SQL tasks only when diagnostics are requested.</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nanasqlite </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NanaSQLite</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NanaSQLite(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;app.db&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">v2_mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">True</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">flush_mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;manual&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    db[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;settings&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;theme&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;dark&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    db.flush(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">wait</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">True</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db.get_status()[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;failed_count&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(db.get_dlq_summary())</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # Resolve the underlying failure before retrying.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        db.retry_dlq()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        db.flush(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">wait</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">True</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>get_dlq_summary()</code> / <code>aget_dlq_summary()</code> returns only the fixed error code <code>write_failed</code> and timestamps. Raw exception text can disclose keys, SQL or values, so the existing engine summary now redacts it as well. Use existing <code>get_dlq()</code> only in a trusted context for details. Retrying or clearing the DLQ does not reset cumulative counters. Evicted failures cannot be retried. Retrying skips old failed KVS operations superseded by a newer write or deletion for the same table/key. This key-level protection does not apply to arbitrary SQL retries.</p><h2 id="bound-asynchronous-submissions" tabindex="-1">Bound asynchronous submissions <a class="header-anchor" href="#bound-asynchronous-submissions" aria-label="Permalink to &quot;Bound asynchronous submissions&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nanasqlite </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> AsyncNanaSQLite</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">():</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> AsyncNanaSQLite(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;app.db&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">max_workers</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        max_pending_operations</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">admission_timeout</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db.aset(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;settings&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;theme&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;dark&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>The optional limit covers running plus queued executor jobs and is shared by table children. Cache-only reads do not need a slot. The default <code>None</code> preserves unbounded admission. A positive <code>admission_timeout</code> raises <code>asyncio.TimeoutError</code> before submission; <code>None</code> waits for capacity. It does not limit SQL execution time.</p><p>Cancellation after submission does not undo a write or free capacity until the actual worker completes. Check the outcome before retrying a cancelled write. Closing rejects new work and drains accepted work even with the default unbounded admission. Cancellation of the caller awaiting <code>close()</code> does not cancel cleanup; await <code>close()</code> again to observe completion. Lazy initialization is also shielded and drained. If an open transaction prevents closing, roll it back or commit it before retrying <code>close()</code>.</p><p>This does not bound user-created waiting coroutines or the separate v2 write-back buffer. Process application input in bounded batches too.</p><h2 id="stream-persisted-pairs" tabindex="-1">Stream persisted pairs <a class="header-anchor" href="#stream-persisted-pairs" aria-label="Permalink to &quot;Stream persisted pairs&quot;">​</a></h2><p><code>db.iter_items(batch_size=256)</code> and <code>db.aiter_items(batch_size=256)</code> yield key/value pairs in key order with bounded keyset pages. Decryption and read hooks are applied without populating the cache. No cursor or connection lock survives a yield. Memory also depends on individual value sizes.</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NanaSQLite(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;app.db&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> key, value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db.iter_items(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">256</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        process(key, value)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Your application&#39;s consumer</span></span></code></pre></div><p>Write-back modes flush first and reject unresolved DLQ entries. Iteration is not a whole-export snapshot: changes may affect later pages, and insertions behind the current key are omitted. Iterate a verified <code>backup()</code> database when a stable snapshot is needed.</p><h2 id="automatic-cache-consistency" tabindex="-1">Automatic cache consistency <a class="header-anchor" href="#automatic-cache-consistency" aria-label="Permalink to &quot;Automatic cache consistency&quot;">​</a></h2><p>Set <code>cache_consistency=&quot;auto&quot;</code> on sync or async constructors. The default <code>manual</code> retains explicit <code>refresh()</code>. Auto mode checks other-connection commits using <a href="https://www.sqlite.org/pragma.html#pragma_data_version" target="_blank" rel="noreferrer">SQLite <code>PRAGMA data_version</code></a>, plus shared-connection writes using <code>total_changes()</code> and transaction state. Changed versions invalidate positive and negative cache entries and hook indexes. Children inherit this option.</p><p>Checks and cache use share a lock. Transactional entries are not reused, protecting against rollback on another wrapper sharing the connection. This is freshness at a check, not snapshot isolation across reads or new cross-process atomicity for hook constraints.</p><p>Auto mode supports immediate persistence, including normal TTL/LRU caches and encryption. It rejects v2, memory-first and persistence TTL, avoiding conflicts with unsaved values and expiration management. Polling costs extra time, and own-connection writes also invalidate caches; retain manual mode for the fastest cached reads.</p>`,21)])])}const u=i(n,[["render",o]]);export{c as __pageData,u as default};
